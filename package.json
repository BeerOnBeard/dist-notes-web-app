{
  "name": "dist-notes-web-app",
  "version": "1.0.0",
  "description": "Application for a user's notes in the Dockerized Identity Server Test (DIST) system.",
  "main": "index.js",
  "scripts": {
    "postinstall": "n-copy --source ./node_modules/knockout/build/output/ --destination ./public/ '*'",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Fournier",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "knockout": "^3.4.2",
    "mongodb": "^2.2.33"
  },
  "devDependencies": {
    "npm-build-tools": "^2.2.5"
  }
}
